<div class="container my-5">
  <div class="text-center mb-2">
    <h1 class="text-primary">Add a New Candidate</h1>
    <p class="text-muted">Fill in the details below to register a new candidate for the election.</p>
  </div>

  <div class="card shadow-sm">
    <div class="card-body">
      <%= render 'form' %>
    </div>
  </div>

  <div class="text-center mt-4">
    <%= link_to 'Back to Candidates List', constituency_candidates_path(@constituency), class: 'btn btn-secondary' %>
  </div>
</div>
